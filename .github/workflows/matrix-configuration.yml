name: Matrix Configuration

on:
 push

jobs:
    deploy-on-ubuntu:
        runs-on: ubuntu-latest
        steps:
        - name: Echo Docker Details
          run: docker info
        - name: Run Images
          run: docker images
    deploy-on-windows:
        runs-on: windows-latest
        steps:
        - name: Echo Docker Details
          run: docker info